<view class="class-lists">
  <view bindtap="goList" data-id="{{item.class_id}}" class="class-item" wx:for="{{classInfo}}" wx:key="index">
    <view class="img-cont">
      <image src="{{item.pic}}" mode="aspectFill"></image>
    </view>
    <view class="name">{{item.name}}</view>
  </view>
</view>
